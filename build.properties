#Properties file for build.xml
project.name=zvrGUI

#Version number for current zvrGUI
zvrgui.ver.num=0.9
project.name.versioned=${project.name}-${zvrgui.ver.num}

#build locations
asdoc.loc=${FLEX_HOME}/bin/asdoc
main.src.loc=${basedir}/src
test.src.loc=${basedir}/test
doc.loc=${basedir}/doc
lib.loc=${basedir}/lib
bin.loc=${basedir}/bin
report.loc=${basedir}/report
dist.loc=${basedir}/dist
template.loc=${basedir}/build/templates